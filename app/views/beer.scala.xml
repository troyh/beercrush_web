@(beer:models.Beer)
@main {
	<beer>
		<name>@beer.name</name>
		<brewery>@beer.brewery.map(_.name)</brewery>
		<desc>@beer.description</desc>
		<styles>@beer.beerStyles.map(s => <style>{s.name}</style>)</styles>
		<abv>@beer.abv</abv>
		<ibu>@beer.ibu</ibu>
	</beer>
}

